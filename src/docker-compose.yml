name: "mssproto"

services:
  
  mssproto-ek:
    container_name: mssproto-ek
    image: nshou/elasticsearch-kibana:latest

  mssproto-rabbitmq:
    container_name: mssproto-rabbitmq
    image: masstransit/rabbitmq

  mssproto-sqldb:
    container_name: mssproto-sqldb
    image: mcr.microsoft.com/mssql/server:2019-latest 

  mssproto-fe:
    image: ${REGISTRY:-mssdevlab}/mssproto:webmvc-${PLATFORM:-linux}-${TAG:-latest}
    container_name: mssproto-fe
    build:
      context: .
      dockerfile: Web/WebMVC/Dockerfile

  mssproto-vk-service:
    image: ${REGISTRY:-mssdevlab}/mssproto:vkservice-${PLATFORM:-linux}-${TAG:-latest}
    container_name: mssproto-vk-service
    build:
      context: .
      dockerfile: Services/VkService/Dockerfile

  mssproto-test-service:
    image: ${REGISTRY:-mssdevlab}/mssproto:testservice-${PLATFORM:-linux}-${TAG:-latest}
    container_name: mssproto-test-service
    build:
      context: .
      dockerfile: Services/TestService/Dockerfile

  mssproto-test-service1:
    image: ${REGISTRY:-mssdevlab}/mssproto:testservice1-${PLATFORM:-linux}-${TAG:-latest}
    container_name: mssproto-test-service1
    build:
      context: .
      dockerfile: Services/TestService1/Dockerfile

  mssproto-test-ad-service:
    image: ${REGISTRY:-mssdevlab}/mssproto:testadservice-${PLATFORM:-linux}-${TAG:-latest}
    container_name: mssproto-test-ad-service
    build:
      context: .
      dockerfile: Services/TestAdService/Dockerfile
